// import { db } from '../../../database/index.ts';

// const queryUserTransactions = (userId) => {
//     return db
//         .selectFrom('prosper.transaction_records')
//         .innerJoin(

//         )
// };

// export const getUserTransactions = async (userId, limit, offset) => {
// };
